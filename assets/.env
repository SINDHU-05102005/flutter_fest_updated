# Supabase configuration.
# Production values are injected on Render via the SUPABASE_URL / SUPABASE_KEY
# environment variables (--dart-define at build time). For local development,
# paste your anon key values here (do NOT commit the real values).
SUPABASE_URL=
SUPABASE_KEY=
